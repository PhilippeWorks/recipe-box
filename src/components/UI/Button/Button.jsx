import React from 'react';

const Button = (props) => (
	<button 
		style={padding: 1%; margin: 1%; background-color: gray;}
		onClick={this.props.handler}>{this.props.label}</button>
);

export default Button;